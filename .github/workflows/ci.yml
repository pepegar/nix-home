name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2.3.4
      with:
        submodules: true
    - uses: cachix/install-nix-action@v12
    - run: nix-env -i home-manager -f '<nixpkgs>'
    - run: home-manager switch
